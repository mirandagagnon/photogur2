<h1>Photos</h1>

<p><%= link_to "New Photo", new_photo_path %></p>

<% @photos.each do |photo| %>
  <p><%= photo.title %>, <%= photo.photographer %></p>
  <%= link_to image_tag(photo.url), photo_path(photo) %>
<% end %>
